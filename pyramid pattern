#include<stdio.h>
int main()
{
	int rows,i,j,k;
	printf("enter the no of rows:");
	scanf("%d",&rows);
	for(i=1;i<=rows;i++){
		for(j=1;j<=i;j++){
			printf(" ");
		}
		for(k=0;k<=i;k++){
				printf("* ");
			}printf("\n");
	}return 0;
}
